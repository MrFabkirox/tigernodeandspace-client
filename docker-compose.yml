version: '3'
services:
  app:
    container_name: tigernodeandspace-client
    restart: always
    build: .
    ports:
      - '3000:3000'
    depends_on:
      - tigernodeandspace
  tigernodeandspace:
    image: tigernodeandspace

